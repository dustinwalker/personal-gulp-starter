//
// LOCAL ONLY SETTINGS
// Copy this sample file to local.config.js so that
// you can have unique settings.
// If you add a local only variable be sure to copy
// that to the sample file so that others on the repo
// can use it.
// THE NON-SAMPLE file IS NOT committed to the repo.

var localconfig = {}

localconfig.devUrl          = 'gulp2b.dev';

module.exports = localconfig;
